// TEMPORARY testing mode.
// While true, visitors are silently signed in as anonymous users so the whole
// app is usable without logging in. Flip to false to restore the normal
// Discord sign-in flow (and disable anonymous sign-ins in the backend).
export const AUTH_DISABLED = true;
